Implement remaining GPS completion features cohesively, working directly in the current workspace. You own gps.go and related sem/gitutil files as needed. Requirements: (1) improve immutable committed capture and detect input changes where practical, yielding explicit INPUT_CHANGED/incomplete response rather than clean result; (2) add symbol-level base/head delta findings joined to GPS anchors/test mappings, avoiding pure changed-file false positives and handling deletions; (3) add opt-in, bounded Git history/checkpoint provenance to GPS why/review or context with explicit HISTORY_UNAVAILABLE state. Keep schemas additive and no-egress. Add focused tests and docs. Do not commit/push. Verify relevant tests/build. Return concise summary/files/tests/unresolved limitations.